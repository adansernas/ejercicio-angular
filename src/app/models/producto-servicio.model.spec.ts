import { ProductoServicio } from './producto-servicio.model';

describe('ProductoServicio', () => {
  it('should create an instance', () => {
    expect(new ProductoServicio()).toBeTruthy();
  });
});
